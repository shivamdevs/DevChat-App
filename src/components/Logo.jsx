import React from 'react';

function Logo({ color }) {
    return (
        <h1 className="inline-block text-purple-700 font-black text-2xl" style={{ color }}>DevChat</h1>
    );
}

export default Logo;
